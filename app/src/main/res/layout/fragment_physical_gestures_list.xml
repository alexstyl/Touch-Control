<?xml version="1.0" encoding="utf-8"?>

<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ListView
        android:id="@android:id/list"
        android:layout_width="match_parent"
        android:scrollbarStyle="outsideOverlay"
        android:divider="?android:attr/listDivider"
        android:layout_height="match_parent"/>

    <!--<LinearLayout-->

    <!--android:layout_width="match_parent"-->
    <!--android:layout_height="wrap_content"-->

    <!--android:id="@+id/physical_list"-->
    <!--android:orientation="vertical">-->

        <!--flipCheck = new CheckListRow(getActivity());-->
        <!--flipCheck.setTitle(R.string.flip_title);-->
        <!--flipCheck.setSummary(R.string.flip_summary);-->


        <!--TODO Change to RecyclerView instead of LinearLayout, for flexibility -->
    <!--</LinearLayout>-->


    <TextView
        android:paddingTop="4dp"
        android:paddingBottom="4dp"
        android:id="@+id/disabled_sensors"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:background="@color/red_warning"
        android:textAppearance="@style/TextAppearance.AppCompat.Medium.Inverse"
        android:textStyle="bold"
        android:fontFamily="sans-serif-condenced"
        android:layout_gravity="bottom"
        android:text="@string/sensors_disabled"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
</FrameLayout>